---
description: ARS academic-paper `format-convert` mode — convert to LaTeX / DOCX / PDF / Markdown
argument-hint: "<input> <target-format>"
---

Load and apply the `academic-paper` skill (provided by the `pi-academic-research` package; in Pi invoke `/skill:academic-paper` or read `upstream/academic-paper/SKILL.md`) and run it in **format-convert mode**.

Converts a paper between LaTeX, DOCX (via Pandoc), PDF, or Markdown, and converts citation styles between major formats. Fidelity spectrum, low oversight.

Mode reference: `MODE_REGISTRY.md` § academic-paper.
Skill entry: `academic-paper/SKILL.md`.

---

[Optional user input]: $@
