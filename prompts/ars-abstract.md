---
description: ARS academic-paper `abstract-only` mode — bilingual abstract + keywords
argument-hint: "[paper / draft path]"
---

Load and apply the `academic-paper` skill (provided by the `pi-academic-research` package; in Pi invoke `/skill:academic-paper` or read `upstream/academic-paper/SKILL.md`) and run it in **abstract-only mode**.

Produces a bilingual (zh-TW + EN) abstract plus keywords. Fidelity spectrum, medium oversight. Carries the v3.6.7 `report_compiler_agent` PATTERN PROTECTION layer when invoked through the pipeline.

Mode reference: `MODE_REGISTRY.md` § academic-paper.
Skill entry: `academic-paper/SKILL.md`.

---

[Optional user input]: $@
